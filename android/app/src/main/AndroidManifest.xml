<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Welle-8 Round 23: Auto-Save in Download/Finanzgame/.
         Survives uninstall + re-install for save restore. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- MANAGE_EXTERNAL_STORAGE ist bewusst NICHT deklariert. Die Sicherung
         laeuft ueber das Storage Access Framework (MainActivity.kt, Channel
         finanzgame/saf): der Nutzer waehlt einmal einen Ordner, wir halten
         eine persistente URI-Berechtigung. Das reicht fuer Schreiben, Lesen
         und Deinstall-Ueberleben; "Zugriff auf alle Dateien" brauchte es
         nie, angefragt hat es zuletzt niemand mehr (der Request-Pfad war
         toter Code), und in einer Kinder-App ist es die denkbar breiteste
         Berechtigung. Bestandsstaende in Download/Finanzgame/ bleiben
         erreichbar: im SAF-Picker genau diesen Ordner waehlen, readBackup
         findet die autosave.fgsave dort unter ihrem gewohnten Namen. -->
    <!-- allowBackup=false: Google Auto-Backup wuerde eine (evtl. veraltete)
         Cloud-DB nach Reinstall wiederherstellen und damit den eigenen
         SAF/Download-Restore-Flow umgehen (onboardingComplete=true =>
         _maybeOfferRestore laeuft nie). Eigene Save-Mechanik ist die
         einzige Quelle der Wahrheit. -->
    <application
        android:label="Finanzgame"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:appCategory="game"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Spielstand-Datei aus WhatsApp/Dateien/Drive mit "Öffnen" an
                 Finanzgame schicken. file:// matcht per Endung *.fgsave;
                 content:// (WhatsApp) matcht per MIME (octet-stream/zip), da
                 der Absender die Endung nicht in die URI schreibt. -->
            <intent-filter android:label="Finanzgame Spielstand">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:host="*"/>
                <data android:mimeType="*/*"/>
                <data android:pathPattern=".*\\.fgsave"/>
                <data android:pathPattern=".*\\..*\\.fgsave"/>
            </intent-filter>
            <intent-filter android:label="Finanzgame Spielstand">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="content"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="application/zip"/>
                <data android:mimeType="application/x-sqlite3"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: externen Browser für den freiwilligen Spende-Link. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
